# 重构方案：news.zjkl.qzz.io 全面改造

> 目标：将线上站点外壳改造成 newsnow 项目的视觉与交互风格，把现有全部功能（热点抓取、自定义订阅、AI 分析、多通道推送）完整接入，重构架构并优化推送调度与消息内容。
> 状态：待你确认「消息推送面板」与补充项后进入实现。

---

## 一、线上对照

| 维度 | newsnow 项目（目标样式） | 当前 worker 仪表盘 |
| --- | --- | --- |
| 视觉 | 深色、极简、留白，强调"优雅阅读"；禁用亮色 | 深色毛玻璃 + 渐变背景、卡片列表 |
| 布局 | 顶部标题 + 底部标签页数据中心，按来源 tab 切换 | 单页上下长列表、分 section |
| 技术 | Vue SPA @ nitro（服务端） | Hono 服务端渲染单页 |
| 实时 | 轮询刷新、来源自适应抓取 | 15 分钟 cron 抓取 |

**结论**：外壳重做为主站；把实时热点 + 订阅 + AI 摘要的数据面板按 newsnow 的"标签页 + 数据源分组"呈现，同时保留控制面板（订阅管理、推送配置、日志）作为二级入口。

---

## 二、目标访问域名布局（newsnow 风格）

- 顶部：站点名 + 日期，保持极简。
- 主区：**标签页**切换——「热榜 / 订阅 / AI 摘要 / 关于」，各 tab 内按数据源分组（微博·抖音·知乎·B站·贴吧·财联社…），每条含标题、排名、来源 tag，可点击外链。
- 底部：数据抓取状态、控制面板入口、RSS。
- 深色为主，暗色渐变替换为 newsnow 的克制纯色；不再弹多套主题。

---

## 三、架构重构（拆解单函数流水线）

现状问题：`runPipeline` 一个函数串联抓取/筛选/渲染/翻译/分析/推送，改一处动全局、易回归（前次 `const top` 即为证明）。

重构为分层：

```
entry(scheduled/fetch)
  └─ ctx = loadContext(env)          // config + schema + 订阅
  ├─ crawl(env, ctx)                 // 平台热榜 + TG/自定义订阅 → 入库(D1)
  ├─ select(env, ctx)                // AI/关键词筛选 + 跨轮去重(pushed_fp)
  ├─ translate(env, ctx)             // 订阅英文→中文(KV 缓存)
  ├─ render(env, ctx)                // renderParts / 站点页
  ├─ summarize(env, ctx)             // aiAnalyze(每日) + 订阅速览digest
  └─ push(env, ctx) + persistLog     // 多通道 + 写 push_records + 结果状态
    └─ 每层独立 try/catch，单层失败不阻断整条
```

职责分离、可单测、可回滚；锁与遥测集中处理。

---

## 四、推送调度（新方案）

- 由「每 15 分钟」改为**每天 4 个整点固定推送**：`08:00 / 12:00 / 17:00 / 20:00`（Asia/Shanghai）。
- 每次推送**一条整合后的完整消息**（见下方消息面板），把当日已抓取内容精炼为一份，避免刷屏与碎片化。
- cron 表达式：`0 8 * * *` / `0 12 * * *` / `0 17 * * *` / `0 20 * * *`（metadata.schedule 改为 4 条）。
- "完整消息"以 **订阅速览 + 平台热榜 + Top + AI 摘要** 合并为一条结构化纯文本（含分组标题），附件图片单独作为一条（≤9 张）。

---

## 五、完整消息推送面板（每日 4 段共用模板，时段仅换头部标签）

```
📰 {站点名} · 8月29日 {早报/午报/晚报/夜报}
──────────────────────────────
【今日热榜】  🔥 平台热搜 Top（按热度）
  1. 微博 ｜ 标题
  2. 抖音 ｜ 标题
  … 各平台各取 Top 3，按平台分组

【📈 重点聚焦】 AI 精选值得关注条目（关键词/兴趣命中）
  • [微博] 标题 — 摘要

【🤖 每日洞察】 （每日首推生成，其余时段复用）
  2-3 句当日大事 + 一句趋势判断

【📡 订阅更新】  （自定义 TG/爆料，AI 速览 digest）
  · 归类的多条精炼，同主题合并
  · 配图随消息附带

【推送说明】 数据来自：… · 可到控制面板调整
```
（企业微信/钉钉/飞书同一结构；图片独立尾推，≤9 张去重）

---

## 六、消息内容优化点

1. 同主题 AI 合并：多平台同一事件合并为一条（含来源），避免重复。
2. 摘要按句号断句（已在上轮启用 `smartClip`）并控制单条 ≤ 合理字数。
3. 平台标签统一中文（已补全映射）。
4. 每段保留来源与链接，便于溯源。
5. 去噪：去掉版权/投稿署名/纯占位媒体标题（已启用 `cleanSubText`）。